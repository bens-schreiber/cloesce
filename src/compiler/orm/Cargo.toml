[package]
name = "orm"
version = "0.0.1"
edition = "2024"

[dependencies]
ast = { path = "../ast" }
sea-query = { workspace = true }

[dev-dependencies]
sqlx = { version = "0.8", features = ["sqlite", "runtime-tokio"] }
