// GENERATED BY CLOESCE. DO NOT MODIFY.
import { HttpResult, KValue, CloesceApp, Orm as CloesceOrm, IncludeTree, DeepPartial, CloesceResult, CloesceError, SqlStatement, applyDurableMigrations, DurableMigration } from "cloesce";
import { DurableObject } from "cloudflare:workers";

/** @internal */
export type MaybePromise<T> = T | Promise<T>;
/** @internal */
export type MaybeHttpResult<T> = T | HttpResult<T>;

/**
 * Return type for all API method implementations.
 *
 * Accepts a raw `T`, an {@link HttpResult}<T>, or a `Promise` of either.
 * Only mark your method `async` if it actually awaits something.
 *
 * To signal an error, either `throw` or return a non-200 {@link HttpResult}. Build results with
 * the `HttpResult.ok(status, data?)` / `HttpResult.fail(status, message?)` statics, imported from
 * the `cloesce` runtime package (the generated namespaces come from `@cloesce/backend.js`):
 *
 * ```ts
 * import * as clo from "@cloesce/backend.js";
 * import { HttpResult } from "cloesce";
 * ```
 *
 * @example
 * ```ts
 * export const User = clo.User.impl({
 *   // Synchronous: return a raw value
 *   displayName(self) {
 *     return `@${self.username}`;
 *   },
 *
 *   // Async: return an HttpResult
 *   async rename(self, env, name) {
 *     const saved = await User.Default.save(env, { ...self, name });
 *     return HttpResult.ok(200, saved.data!);
 *   },
 *
 *   // Error: throw or return a non-200 HttpResult
 *   delete(self, env) {
 *     if (!allowed) return HttpResult.fail(403, "Forbidden");
 *     // ...
 *   },
 * });
 * ```
 */
export type ApiResult<T> = MaybePromise<MaybeHttpResult<T>>;

/**
 * Raw Cloudflare Workers environment with one property per `wrangler.toml` binding.
 *
 * Does **not** include Cloesce helpers. Prefer {@link Env} in your handlers.
 *
 * @see {@link Env}
 */
export interface CfEnv {
    db: D1Database;
}

/** @internal Per-binding upgraded types. Use the top-level {@link Env} type instead. */
export namespace Env {
}

/**
 * Cloesce-upgraded environment. This is the `env` you receive in API implementations.
 *
 * Extends {@link CfEnv} with typed helpers on each binding:
 * - **KV / R2**: `env.MyKv.<field>.get/put/list` (raw Cloudflare API still available)
 * - **Durable Object**: `env.MyDo.stub<MyDoImpl>(...)` / `template` / `id`
 *
 * @remarks
 * The injected `env` is guaranteed to contain everything declared in your schema.
 * A missing binding throws a 500 at runtime. When inside a DO data source `get`,
 * `env` also contains a `ctx` property with the Durable Object context.
 *
 * @example KV field helpers (`<binding>.<field>.get/put/list`)
 * ```ts
 * // For a KV field declared on a binding `Sessions`:
 * const token = "abc";
 * await env.Sessions.session.put(token, username);   // typed value
 * const username = await env.Sessions.session.get(token);
 * const { keys } = await env.Sessions.session.list();
 *
 * // The raw Cloudflare KV API is still available on the same binding:
 * await env.Sessions.delete(env.Sessions.session.template(token));
 * ```
 *
 * @example R2 field helpers
 * ```ts
 * // For an R2 field `avatar` declared on a binding `Avatars`:
 * await env.Avatars.avatar.put(userId, bytes);
 * const obj = await env.Avatars.avatar.get(userId); // R2ObjectBody | null
 * ```
 *
 * @example Durable Object stub (always pass `<T>`)
 * ```ts
 * const stub = env.CounterDo.stub<CounterDo>(tenant);
 * await stub.increment();
 * ```
 */
export type Env = CfEnv & {
};

/**
 * Upgrade a raw {@link CfEnv} into a Cloesce {@link Env}.
 *
 * Called automatically by the generated entrypoint. Only needed for custom `fetch` handlers.
 *
 * @param env - The raw Cloudflare Workers environment to upgrade.
 * @returns The same object, mutated in place with Cloesce helpers attached.
 */
export function upgradeEnv(env: CfEnv): Env {
    return env as Env;
}
/**
 * Generated surface for the `A` model.
 *
 * Implement via {@link impl}. Access data through data sources, not this namespace directly.
 *
 * @example
 * ```ts
 * export const A = clo.A.impl({
 *   async someApi(self, env, arg) {
 *     const row = await A.Default.get(env, arg);
 *     return this.otherApi(self, env);
 *   },
 * });
 * ```
 */
export namespace A {
    /** @internal */
    export const Tag = "A" as const;
    /** @internal */
    export const Meta = cidl.models.A as any;

    /** Hydrated row type. Passed as `self` to instance methods and returned by data-source reads. */
    export interface Self {
        id: number;
        bId: number | null;
        b: B.Self;
    }

    /** API methods to implement. Instance methods receive `self`; static methods do not. Each returns an {@link ApiResult}. */
    export interface Api {
        create(
            env: {
                db: Env["db"],
            },
            a: A.Self,
        ): ApiResult<A.Self>;
        withoutB(
            self: A.Self,
        ): ApiResult<A.Self>;
    }
    /** @internal */
    export const _api = undefined as unknown as Api;

    /** Data sources with custom (stubbed) CRUD that you must implement. */
    export interface Sources {
    }

    /**
     * Generated data sources with ready-to-call `get` / `list` / `save` operations.
     *
     * Prefer these over {@link Orm}. Use `Orm` only when you need a custom include tree or query.
     */
    export namespace GeneratedSource {
        /** The `Default` data source. Provides `get`, `list`, and `save` operations. */
        export const Default = {
            /** The include tree this data source hydrates. */
            tree: {"b":{"a":{}}},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "A"."id" AS "id", "A"."bId" AS "bId", "B_1"."id" AS "b.id", "A_2"."id" AS "b.a.id", "A_2"."bId" AS "b.a.bId" FROM "A" LEFT JOIN "B" AS "B_1" ON "A"."bId" = "B_1"."id" LEFT JOIN "A" AS "A_2" ON "B_1"."id" = "A_2"."bId"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "A"."id" AS "id", "A"."bId" AS "bId", "B_1"."id" AS "b.id", "A_2"."id" AS "b.a.id", "A_2"."bId" AS "b.a.bId" FROM "A" LEFT JOIN "B" AS "B_1" ON "A"."bId" = "B_1"."id" LEFT JOIN "A" AS "A_2" ON "B_1"."id" = "A_2"."bId" WHERE "A"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "A"."id" AS "id", "A"."bId" AS "bId", "B_1"."id" AS "b.id", "A_2"."id" AS "b.a.id", "A_2"."bId" AS "b.a.bId" FROM "A" LEFT JOIN "B" AS "B_1" ON "A"."bId" = "B_1"."id" LEFT JOIN "A" AS "A_2" ON "B_1"."id" = "A_2"."bId" WHERE "A"."id" > ?1 ORDER BY "A"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `A`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `A` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `A` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
        /** The `WithB` data source. Provides `get`, `list`, and `save` operations. */
        export const WithB = {
            /** The include tree this data source hydrates. */
            tree: {"b":{}},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "A"."id" AS "id", "A"."bId" AS "bId", "B_1"."id" AS "b.id" FROM "A" LEFT JOIN "B" AS "B_1" ON "A"."bId" = "B_1"."id"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "A"."id" AS "id", "A"."bId" AS "bId", "B_1"."id" AS "b.id" FROM "A" LEFT JOIN "B" AS "B_1" ON "A"."bId" = "B_1"."id" WHERE "A"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "A"."id" AS "id", "A"."bId" AS "bId", "B_1"."id" AS "b.id" FROM "A" LEFT JOIN "B" AS "B_1" ON "A"."bId" = "B_1"."id" WHERE "A"."id" > ?1 ORDER BY "A"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `A`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `A` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `A` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
        /** The `WithoutB` data source. Provides `get`, `list`, and `save` operations. */
        export const WithoutB = {
            /** The include tree this data source hydrates. */
            tree: {},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "A"."id" AS "id", "A"."bId" AS "bId" FROM "A"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "A"."id" AS "id", "A"."bId" AS "bId" FROM "A" WHERE "A"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "A"."id" AS "id", "A"."bId" AS "bId" FROM "A" WHERE "A"."id" > ?1 ORDER BY "A"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `A`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `A` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `A` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
    }

    /**
     * Register your implementation of `A`.
     *
     * Use `this` to call sibling methods and access generated data sources.
     *
     * @param implObj - Object implementing each {@link Api} method and any custom {@link Sources}.
     * @returns The bound implementation that Cloesce dispatches requests to.
     *
     * @example
     * ```ts
     * export const A = clo.A.impl({
     *   async myApi(self, env, arg) {
     *     const res = await A.Default.get(env, arg);
     *     return res.data!;
     *   },
     * });
     * ```
     */
    export function impl<Impl extends Api & Sources>(implObj: Impl & ThisType<{ tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default; WithB: typeof GeneratedSource.WithB; WithoutB: typeof GeneratedSource.WithoutB } & Impl>): { tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default; WithB: typeof GeneratedSource.WithB; WithoutB: typeof GeneratedSource.WithoutB } & Impl {
        return _impl("A", { Orm, Default: GeneratedSource.Default, WithB: GeneratedSource.WithB, WithoutB: GeneratedSource.WithoutB }, implObj) as any;
    }

    /**
     * Lower-level ORM access with explicit {@link IncludeTree}. Returns raw `CloesceResult`.
     *
     * Prefer data sources (e.g. `A.Default.save(...)`) unless you need a custom tree or query.
     */
    export namespace Orm {
        /**
         * Insert or update `A` and its included relations.
         *
         * @param env - Environment with the required database binding(s).
         * @param newModel - Partial row data to upsert.
         * @param include - Include tree controlling which relations to persist. Defaults to `Default.tree`.
         * @returns The saved row, or `null` if the target vanished.
         */
        export async function save(env: { db: CfEnv["db"] }, newModel: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self | null>> {
            return await CloesceOrm.fromEnv(env).upsert<Self>(Meta, newModel, include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate the first result.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns The hydrated row, or `null` if no row matches.
         */
        export async function get(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Self | null>> {
            args.include ??= GeneratedSource.Default.tree
            return await CloesceOrm.fromEnv(env).get<Self>(Meta, args.query, args.include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate all results.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns All matching hydrated rows.
         */
        export async function list(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<A.Self[]>> {
            args.include ??= GeneratedSource.Default.tree;
            return await CloesceOrm.fromEnv(env).list<Self>(Meta, args.query, args.include);
        }

        /**
         * Hydrate an already-fetched `D1Result` into `A` rows.
         *
         * @param result - The raw D1 query result to hydrate.
         * @param include - Include tree controlling nested object assembly. Defaults to `Default.tree`.
         * @returns The hydrated rows.
         */
        export function map(result: D1Result, include: IncludeTree<Self> = GeneratedSource.Default.tree): Self[] {
            return CloesceOrm.map<Self>(Meta, result, include);
        }

        /**
         * Hydrate a partial `A` into a full row, loading the included relations.
         *
         * @param env - Environment with the required binding(s).
         * @param base - Partial row with at least the primary/route key fields populated.
         * @param include - Include tree controlling which relations to load. Defaults to `Default.tree`.
         */
        export async function hydrate(env: { db: CfEnv["db"] }, base: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self>> {
            return await CloesceOrm.fromEnv(env).hydrate<Self>(Meta, base, include);
        }
    }
}
/**
 * Generated surface for the `B` model.
 *
 * Implement via {@link impl}. Access data through data sources, not this namespace directly.
 *
 * @example
 * ```ts
 * export const B = clo.B.impl({
 *   async someApi(self, env, arg) {
 *     const row = await B.Default.get(env, arg);
 *     return this.otherApi(self, env);
 *   },
 * });
 * ```
 */
export namespace B {
    /** @internal */
    export const Tag = "B" as const;
    /** @internal */
    export const Meta = cidl.models.B as any;

    /** Hydrated row type. Passed as `self` to instance methods and returned by data-source reads. */
    export interface Self {
        id: number;
        a: A.Self[];
    }

    /** API methods to implement. Instance methods receive `self`; static methods do not. Each returns an {@link ApiResult}. */
    export interface Api {
        testMethod(
            self: B.Self,
        ): ApiResult<void>;
    }
    /** @internal */
    export const _api = undefined as unknown as Api;

    /** Data sources with custom (stubbed) CRUD that you must implement. */
    export interface Sources {
    }

    /**
     * Generated data sources with ready-to-call `get` / `list` / `save` operations.
     *
     * Prefer these over {@link Orm}. Use `Orm` only when you need a custom include tree or query.
     */
    export namespace GeneratedSource {
        /** The `Default` data source. Provides `get`, `list`, and `save` operations. */
        export const Default = {
            /** The include tree this data source hydrates. */
            tree: {"a":{}},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "B"."id" AS "id", "A_1"."id" AS "a.id", "A_1"."bId" AS "a.bId" FROM "B" LEFT JOIN "A" AS "A_1" ON "B"."id" = "A_1"."bId"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "B"."id" AS "id", "A_1"."id" AS "a.id", "A_1"."bId" AS "a.bId" FROM "B" LEFT JOIN "A" AS "A_1" ON "B"."id" = "A_1"."bId" WHERE "B"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "B"."id" AS "id", "A_1"."id" AS "a.id", "A_1"."bId" AS "a.bId" FROM "B" LEFT JOIN "A" AS "A_1" ON "B"."id" = "A_1"."bId" WHERE "B"."id" > ?1 ORDER BY "B"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `B`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `B` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `B` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
    }

    /**
     * Register your implementation of `B`.
     *
     * Use `this` to call sibling methods and access generated data sources.
     *
     * @param implObj - Object implementing each {@link Api} method and any custom {@link Sources}.
     * @returns The bound implementation that Cloesce dispatches requests to.
     *
     * @example
     * ```ts
     * export const B = clo.B.impl({
     *   async myApi(self, env, arg) {
     *     const res = await B.Default.get(env, arg);
     *     return res.data!;
     *   },
     * });
     * ```
     */
    export function impl<Impl extends Api & Sources>(implObj: Impl & ThisType<{ tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default } & Impl>): { tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default } & Impl {
        return _impl("B", { Orm, Default: GeneratedSource.Default }, implObj) as any;
    }

    /**
     * Lower-level ORM access with explicit {@link IncludeTree}. Returns raw `CloesceResult`.
     *
     * Prefer data sources (e.g. `B.Default.save(...)`) unless you need a custom tree or query.
     */
    export namespace Orm {
        /**
         * Insert or update `B` and its included relations.
         *
         * @param env - Environment with the required database binding(s).
         * @param newModel - Partial row data to upsert.
         * @param include - Include tree controlling which relations to persist. Defaults to `Default.tree`.
         * @returns The saved row, or `null` if the target vanished.
         */
        export async function save(env: { db: CfEnv["db"] }, newModel: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self | null>> {
            return await CloesceOrm.fromEnv(env).upsert<Self>(Meta, newModel, include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate the first result.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns The hydrated row, or `null` if no row matches.
         */
        export async function get(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Self | null>> {
            args.include ??= GeneratedSource.Default.tree
            return await CloesceOrm.fromEnv(env).get<Self>(Meta, args.query, args.include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate all results.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns All matching hydrated rows.
         */
        export async function list(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<B.Self[]>> {
            args.include ??= GeneratedSource.Default.tree;
            return await CloesceOrm.fromEnv(env).list<Self>(Meta, args.query, args.include);
        }

        /**
         * Hydrate an already-fetched `D1Result` into `B` rows.
         *
         * @param result - The raw D1 query result to hydrate.
         * @param include - Include tree controlling nested object assembly. Defaults to `Default.tree`.
         * @returns The hydrated rows.
         */
        export function map(result: D1Result, include: IncludeTree<Self> = GeneratedSource.Default.tree): Self[] {
            return CloesceOrm.map<Self>(Meta, result, include);
        }

        /**
         * Hydrate a partial `B` into a full row, loading the included relations.
         *
         * @param env - Environment with the required binding(s).
         * @param base - Partial row with at least the primary/route key fields populated.
         * @param include - Include tree controlling which relations to load. Defaults to `Default.tree`.
         */
        export async function hydrate(env: { db: CfEnv["db"] }, base: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self>> {
            return await CloesceOrm.fromEnv(env).hydrate<Self>(Meta, base, include);
        }
    }
}
/**
 * Generated surface for the `Course` model.
 *
 * Implement via {@link impl}. Access data through data sources, not this namespace directly.
 *
 * @example
 * ```ts
 * export const Course = clo.Course.impl({
 *   async someApi(self, env, arg) {
 *     const row = await Course.Default.get(env, arg);
 *     return this.otherApi(self, env);
 *   },
 * });
 * ```
 */
export namespace Course {
    /** @internal */
    export const Tag = "Course" as const;
    /** @internal */
    export const Meta = cidl.models.Course as any;

    /** Hydrated row type. Passed as `self` to instance methods and returned by data-source reads. */
    export interface Self {
        id: number;
        students: CourseStudent.Self[];
    }

    /** API methods to implement. Instance methods receive `self`; static methods do not. Each returns an {@link ApiResult}. */
    export interface Api {
    }
    /** @internal */
    export const _api = undefined as unknown as Api;

    /** Data sources with custom (stubbed) CRUD that you must implement. */
    export interface Sources {
    }

    /**
     * Generated data sources with ready-to-call `get` / `list` / `save` operations.
     *
     * Prefer these over {@link Orm}. Use `Orm` only when you need a custom include tree or query.
     */
    export namespace GeneratedSource {
        /** The `Default` data source. Provides `get`, `list`, and `save` operations. */
        export const Default = {
            /** The include tree this data source hydrates. */
            tree: {"students":{}},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "Course"."id" AS "id", "CourseStudent_1"."courseId" AS "students.courseId", "CourseStudent_1"."studentId" AS "students.studentId" FROM "Course" LEFT JOIN "CourseStudent" AS "CourseStudent_1" ON "Course"."id" = "CourseStudent_1"."courseId"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Course"."id" AS "id", "CourseStudent_1"."courseId" AS "students.courseId", "CourseStudent_1"."studentId" AS "students.studentId" FROM "Course" LEFT JOIN "CourseStudent" AS "CourseStudent_1" ON "Course"."id" = "CourseStudent_1"."courseId" WHERE "Course"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Course"."id" AS "id", "CourseStudent_1"."courseId" AS "students.courseId", "CourseStudent_1"."studentId" AS "students.studentId" FROM "Course" LEFT JOIN "CourseStudent" AS "CourseStudent_1" ON "Course"."id" = "CourseStudent_1"."courseId" WHERE "Course"."id" > ?1 ORDER BY "Course"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `Course`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `Course` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `Course` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
    }

    /**
     * Register your implementation of `Course`.
     *
     * Use `this` to call sibling methods and access generated data sources.
     *
     * @param implObj - Object implementing each {@link Api} method and any custom {@link Sources}.
     * @returns The bound implementation that Cloesce dispatches requests to.
     *
     * @example
     * ```ts
     * export const Course = clo.Course.impl({
     *   async myApi(self, env, arg) {
     *     const res = await Course.Default.get(env, arg);
     *     return res.data!;
     *   },
     * });
     * ```
     */
    export function impl<Impl extends Api & Sources>(implObj: Impl & ThisType<{ tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default } & Impl>): { tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default } & Impl {
        return _impl("Course", { Orm, Default: GeneratedSource.Default }, implObj) as any;
    }

    /**
     * Lower-level ORM access with explicit {@link IncludeTree}. Returns raw `CloesceResult`.
     *
     * Prefer data sources (e.g. `Course.Default.save(...)`) unless you need a custom tree or query.
     */
    export namespace Orm {
        /**
         * Insert or update `Course` and its included relations.
         *
         * @param env - Environment with the required database binding(s).
         * @param newModel - Partial row data to upsert.
         * @param include - Include tree controlling which relations to persist. Defaults to `Default.tree`.
         * @returns The saved row, or `null` if the target vanished.
         */
        export async function save(env: { db: CfEnv["db"] }, newModel: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self | null>> {
            return await CloesceOrm.fromEnv(env).upsert<Self>(Meta, newModel, include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate the first result.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns The hydrated row, or `null` if no row matches.
         */
        export async function get(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Self | null>> {
            args.include ??= GeneratedSource.Default.tree
            return await CloesceOrm.fromEnv(env).get<Self>(Meta, args.query, args.include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate all results.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns All matching hydrated rows.
         */
        export async function list(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Course.Self[]>> {
            args.include ??= GeneratedSource.Default.tree;
            return await CloesceOrm.fromEnv(env).list<Self>(Meta, args.query, args.include);
        }

        /**
         * Hydrate an already-fetched `D1Result` into `Course` rows.
         *
         * @param result - The raw D1 query result to hydrate.
         * @param include - Include tree controlling nested object assembly. Defaults to `Default.tree`.
         * @returns The hydrated rows.
         */
        export function map(result: D1Result, include: IncludeTree<Self> = GeneratedSource.Default.tree): Self[] {
            return CloesceOrm.map<Self>(Meta, result, include);
        }

        /**
         * Hydrate a partial `Course` into a full row, loading the included relations.
         *
         * @param env - Environment with the required binding(s).
         * @param base - Partial row with at least the primary/route key fields populated.
         * @param include - Include tree controlling which relations to load. Defaults to `Default.tree`.
         */
        export async function hydrate(env: { db: CfEnv["db"] }, base: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self>> {
            return await CloesceOrm.fromEnv(env).hydrate<Self>(Meta, base, include);
        }
    }
}
/**
 * Generated surface for the `Person` model.
 *
 * Implement via {@link impl}. Access data through data sources, not this namespace directly.
 *
 * @example
 * ```ts
 * export const Person = clo.Person.impl({
 *   async someApi(self, env, arg) {
 *     const row = await Person.Default.get(env, arg);
 *     return this.otherApi(self, env);
 *   },
 * });
 * ```
 */
export namespace Person {
    /** @internal */
    export const Tag = "Person" as const;
    /** @internal */
    export const Meta = cidl.models.Person as any;

    /** Hydrated row type. Passed as `self` to instance methods and returned by data-source reads. */
    export interface Self {
        id: number;
        dogs: Dog.Self[];
    }

    /** API methods to implement. Instance methods receive `self`; static methods do not. Each returns an {@link ApiResult}. */
    export interface Api {
        create(
            env: {
                db: Env["db"],
            },
            person: DeepPartial<Person.Self>,
        ): ApiResult<Person.Self>;
        withoutDogs(
            self: Person.Self,
        ): ApiResult<Person.Self>;
    }
    /** @internal */
    export const _api = undefined as unknown as Api;

    /** Data sources with custom (stubbed) CRUD that you must implement. */
    export interface Sources {
    }

    /**
     * Generated data sources with ready-to-call `get` / `list` / `save` operations.
     *
     * Prefer these over {@link Orm}. Use `Orm` only when you need a custom include tree or query.
     */
    export namespace GeneratedSource {
        /** The `Default` data source. Provides `get`, `list`, and `save` operations. */
        export const Default = {
            /** The include tree this data source hydrates. */
            tree: {"dogs":{}},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "Person"."id" AS "id", "Dog_1"."id" AS "dogs.id", "Dog_1"."personId" AS "dogs.personId" FROM "Person" LEFT JOIN "Dog" AS "Dog_1" ON "Person"."id" = "Dog_1"."personId"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Person"."id" AS "id", "Dog_1"."id" AS "dogs.id", "Dog_1"."personId" AS "dogs.personId" FROM "Person" LEFT JOIN "Dog" AS "Dog_1" ON "Person"."id" = "Dog_1"."personId" WHERE "Person"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Person"."id" AS "id", "Dog_1"."id" AS "dogs.id", "Dog_1"."personId" AS "dogs.personId" FROM "Person" LEFT JOIN "Dog" AS "Dog_1" ON "Person"."id" = "Dog_1"."personId" WHERE "Person"."id" > ?1 ORDER BY "Person"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `Person`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `Person` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `Person` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
        /** The `WithDogs` data source. Provides `get`, `list`, and `save` operations. */
        export const WithDogs = {
            /** The include tree this data source hydrates. */
            tree: {"dogs":{}},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "Person"."id" AS "id", "Dog_1"."id" AS "dogs.id", "Dog_1"."personId" AS "dogs.personId" FROM "Person" LEFT JOIN "Dog" AS "Dog_1" ON "Person"."id" = "Dog_1"."personId"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Person"."id" AS "id", "Dog_1"."id" AS "dogs.id", "Dog_1"."personId" AS "dogs.personId" FROM "Person" LEFT JOIN "Dog" AS "Dog_1" ON "Person"."id" = "Dog_1"."personId" WHERE "Person"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Person"."id" AS "id", "Dog_1"."id" AS "dogs.id", "Dog_1"."personId" AS "dogs.personId" FROM "Person" LEFT JOIN "Dog" AS "Dog_1" ON "Person"."id" = "Dog_1"."personId" WHERE "Person"."id" > ?1 ORDER BY "Person"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `Person`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `Person` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `Person` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
        /** The `WithoutDogs` data source. Provides `get`, `list`, and `save` operations. */
        export const WithoutDogs = {
            /** The include tree this data source hydrates. */
            tree: {},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "Person"."id" AS "id" FROM "Person"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Person"."id" AS "id" FROM "Person" WHERE "Person"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Person"."id" AS "id" FROM "Person" WHERE "Person"."id" > ?1 ORDER BY "Person"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `Person`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `Person` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `Person` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
    }

    /**
     * Register your implementation of `Person`.
     *
     * Use `this` to call sibling methods and access generated data sources.
     *
     * @param implObj - Object implementing each {@link Api} method and any custom {@link Sources}.
     * @returns The bound implementation that Cloesce dispatches requests to.
     *
     * @example
     * ```ts
     * export const Person = clo.Person.impl({
     *   async myApi(self, env, arg) {
     *     const res = await Person.Default.get(env, arg);
     *     return res.data!;
     *   },
     * });
     * ```
     */
    export function impl<Impl extends Api & Sources>(implObj: Impl & ThisType<{ tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default; WithDogs: typeof GeneratedSource.WithDogs; WithoutDogs: typeof GeneratedSource.WithoutDogs } & Impl>): { tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default; WithDogs: typeof GeneratedSource.WithDogs; WithoutDogs: typeof GeneratedSource.WithoutDogs } & Impl {
        return _impl("Person", { Orm, Default: GeneratedSource.Default, WithDogs: GeneratedSource.WithDogs, WithoutDogs: GeneratedSource.WithoutDogs }, implObj) as any;
    }

    /**
     * Lower-level ORM access with explicit {@link IncludeTree}. Returns raw `CloesceResult`.
     *
     * Prefer data sources (e.g. `Person.Default.save(...)`) unless you need a custom tree or query.
     */
    export namespace Orm {
        /**
         * Insert or update `Person` and its included relations.
         *
         * @param env - Environment with the required database binding(s).
         * @param newModel - Partial row data to upsert.
         * @param include - Include tree controlling which relations to persist. Defaults to `Default.tree`.
         * @returns The saved row, or `null` if the target vanished.
         */
        export async function save(env: { db: CfEnv["db"] }, newModel: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self | null>> {
            return await CloesceOrm.fromEnv(env).upsert<Self>(Meta, newModel, include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate the first result.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns The hydrated row, or `null` if no row matches.
         */
        export async function get(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Self | null>> {
            args.include ??= GeneratedSource.Default.tree
            return await CloesceOrm.fromEnv(env).get<Self>(Meta, args.query, args.include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate all results.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns All matching hydrated rows.
         */
        export async function list(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Person.Self[]>> {
            args.include ??= GeneratedSource.Default.tree;
            return await CloesceOrm.fromEnv(env).list<Self>(Meta, args.query, args.include);
        }

        /**
         * Hydrate an already-fetched `D1Result` into `Person` rows.
         *
         * @param result - The raw D1 query result to hydrate.
         * @param include - Include tree controlling nested object assembly. Defaults to `Default.tree`.
         * @returns The hydrated rows.
         */
        export function map(result: D1Result, include: IncludeTree<Self> = GeneratedSource.Default.tree): Self[] {
            return CloesceOrm.map<Self>(Meta, result, include);
        }

        /**
         * Hydrate a partial `Person` into a full row, loading the included relations.
         *
         * @param env - Environment with the required binding(s).
         * @param base - Partial row with at least the primary/route key fields populated.
         * @param include - Include tree controlling which relations to load. Defaults to `Default.tree`.
         */
        export async function hydrate(env: { db: CfEnv["db"] }, base: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self>> {
            return await CloesceOrm.fromEnv(env).hydrate<Self>(Meta, base, include);
        }
    }
}
/**
 * Generated surface for the `Student` model.
 *
 * Implement via {@link impl}. Access data through data sources, not this namespace directly.
 *
 * @example
 * ```ts
 * export const Student = clo.Student.impl({
 *   async someApi(self, env, arg) {
 *     const row = await Student.Default.get(env, arg);
 *     return this.otherApi(self, env);
 *   },
 * });
 * ```
 */
export namespace Student {
    /** @internal */
    export const Tag = "Student" as const;
    /** @internal */
    export const Meta = cidl.models.Student as any;

    /** Hydrated row type. Passed as `self` to instance methods and returned by data-source reads. */
    export interface Self {
        id: number;
        courses: CourseStudent.Self[];
    }

    /** API methods to implement. Instance methods receive `self`; static methods do not. Each returns an {@link ApiResult}. */
    export interface Api {
        create(
            env: {
                db: Env["db"],
            },
            student: DeepPartial<Student.Self>,
        ): ApiResult<Student.Self>;
        none(
            self: Student.Self,
        ): ApiResult<Student.Self>;
    }
    /** @internal */
    export const _api = undefined as unknown as Api;

    /** Data sources with custom (stubbed) CRUD that you must implement. */
    export interface Sources {
    }

    /**
     * Generated data sources with ready-to-call `get` / `list` / `save` operations.
     *
     * Prefer these over {@link Orm}. Use `Orm` only when you need a custom include tree or query.
     */
    export namespace GeneratedSource {
        /** The `Default` data source. Provides `get`, `list`, and `save` operations. */
        export const Default = {
            /** The include tree this data source hydrates. */
            tree: {"courses":{}},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "Student"."id" AS "id", "CourseStudent_1"."courseId" AS "courses.courseId", "CourseStudent_1"."studentId" AS "courses.studentId" FROM "Student" LEFT JOIN "CourseStudent" AS "CourseStudent_1" ON "Student"."id" = "CourseStudent_1"."studentId"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Student"."id" AS "id", "CourseStudent_1"."courseId" AS "courses.courseId", "CourseStudent_1"."studentId" AS "courses.studentId" FROM "Student" LEFT JOIN "CourseStudent" AS "CourseStudent_1" ON "Student"."id" = "CourseStudent_1"."studentId" WHERE "Student"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Student"."id" AS "id", "CourseStudent_1"."courseId" AS "courses.courseId", "CourseStudent_1"."studentId" AS "courses.studentId" FROM "Student" LEFT JOIN "CourseStudent" AS "CourseStudent_1" ON "Student"."id" = "CourseStudent_1"."studentId" WHERE "Student"."id" > ?1 ORDER BY "Student"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `Student`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `Student` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `Student` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
        /** The `None` data source. Provides `get`, `list`, and `save` operations. */
        export const None = {
            /** The include tree this data source hydrates. */
            tree: {},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "Student"."id" AS "id" FROM "Student"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Student"."id" AS "id" FROM "Student" WHERE "Student"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Student"."id" AS "id" FROM "Student" WHERE "Student"."id" > ?1 ORDER BY "Student"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `Student`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `Student` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `Student` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
        /** The `WithCoursesStudentsCourses` data source. Provides `get`, `list`, and `save` operations. */
        export const WithCoursesStudentsCourses = {
            /** The include tree this data source hydrates. */
            tree: {"courses":{"course":{"students":{"student":{"courses":{}}}}}},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "Student"."id" AS "id", "CourseStudent_1"."courseId" AS "courses.courseId", "CourseStudent_1"."studentId" AS "courses.studentId", "Course_2"."id" AS "courses.course.id", "CourseStudent_3"."courseId" AS "courses.course.students.courseId", "CourseStudent_3"."studentId" AS "courses.course.students.studentId", "Student_4"."id" AS "courses.course.students.student.id", "CourseStudent_5"."courseId" AS "courses.course.students.student.courses.courseId", "CourseStudent_5"."studentId" AS "courses.course.students.student.courses.studentId" FROM "Student" LEFT JOIN "CourseStudent" AS "CourseStudent_1" ON "Student"."id" = "CourseStudent_1"."studentId" LEFT JOIN "Course" AS "Course_2" ON "CourseStudent_1"."courseId" = "Course_2"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_3" ON "Course_2"."id" = "CourseStudent_3"."courseId" LEFT JOIN "Student" AS "Student_4" ON "CourseStudent_3"."studentId" = "Student_4"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_5" ON "Student_4"."id" = "CourseStudent_5"."studentId"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Student"."id" AS "id", "CourseStudent_1"."courseId" AS "courses.courseId", "CourseStudent_1"."studentId" AS "courses.studentId", "Course_2"."id" AS "courses.course.id", "CourseStudent_3"."courseId" AS "courses.course.students.courseId", "CourseStudent_3"."studentId" AS "courses.course.students.studentId", "Student_4"."id" AS "courses.course.students.student.id", "CourseStudent_5"."courseId" AS "courses.course.students.student.courses.courseId", "CourseStudent_5"."studentId" AS "courses.course.students.student.courses.studentId" FROM "Student" LEFT JOIN "CourseStudent" AS "CourseStudent_1" ON "Student"."id" = "CourseStudent_1"."studentId" LEFT JOIN "Course" AS "Course_2" ON "CourseStudent_1"."courseId" = "Course_2"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_3" ON "Course_2"."id" = "CourseStudent_3"."courseId" LEFT JOIN "Student" AS "Student_4" ON "CourseStudent_3"."studentId" = "Student_4"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_5" ON "Student_4"."id" = "CourseStudent_5"."studentId" WHERE "Student"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Student"."id" AS "id", "CourseStudent_1"."courseId" AS "courses.courseId", "CourseStudent_1"."studentId" AS "courses.studentId", "Course_2"."id" AS "courses.course.id", "CourseStudent_3"."courseId" AS "courses.course.students.courseId", "CourseStudent_3"."studentId" AS "courses.course.students.studentId", "Student_4"."id" AS "courses.course.students.student.id", "CourseStudent_5"."courseId" AS "courses.course.students.student.courses.courseId", "CourseStudent_5"."studentId" AS "courses.course.students.student.courses.studentId" FROM "Student" LEFT JOIN "CourseStudent" AS "CourseStudent_1" ON "Student"."id" = "CourseStudent_1"."studentId" LEFT JOIN "Course" AS "Course_2" ON "CourseStudent_1"."courseId" = "Course_2"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_3" ON "Course_2"."id" = "CourseStudent_3"."courseId" LEFT JOIN "Student" AS "Student_4" ON "CourseStudent_3"."studentId" = "Student_4"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_5" ON "Student_4"."id" = "CourseStudent_5"."studentId" WHERE "Student"."id" > ?1 ORDER BY "Student"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `Student`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `Student` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `Student` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
    }

    /**
     * Register your implementation of `Student`.
     *
     * Use `this` to call sibling methods and access generated data sources.
     *
     * @param implObj - Object implementing each {@link Api} method and any custom {@link Sources}.
     * @returns The bound implementation that Cloesce dispatches requests to.
     *
     * @example
     * ```ts
     * export const Student = clo.Student.impl({
     *   async myApi(self, env, arg) {
     *     const res = await Student.Default.get(env, arg);
     *     return res.data!;
     *   },
     * });
     * ```
     */
    export function impl<Impl extends Api & Sources>(implObj: Impl & ThisType<{ tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default; None: typeof GeneratedSource.None; WithCoursesStudentsCourses: typeof GeneratedSource.WithCoursesStudentsCourses } & Impl>): { tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default; None: typeof GeneratedSource.None; WithCoursesStudentsCourses: typeof GeneratedSource.WithCoursesStudentsCourses } & Impl {
        return _impl("Student", { Orm, Default: GeneratedSource.Default, None: GeneratedSource.None, WithCoursesStudentsCourses: GeneratedSource.WithCoursesStudentsCourses }, implObj) as any;
    }

    /**
     * Lower-level ORM access with explicit {@link IncludeTree}. Returns raw `CloesceResult`.
     *
     * Prefer data sources (e.g. `Student.Default.save(...)`) unless you need a custom tree or query.
     */
    export namespace Orm {
        /**
         * Insert or update `Student` and its included relations.
         *
         * @param env - Environment with the required database binding(s).
         * @param newModel - Partial row data to upsert.
         * @param include - Include tree controlling which relations to persist. Defaults to `Default.tree`.
         * @returns The saved row, or `null` if the target vanished.
         */
        export async function save(env: { db: CfEnv["db"] }, newModel: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self | null>> {
            return await CloesceOrm.fromEnv(env).upsert<Self>(Meta, newModel, include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate the first result.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns The hydrated row, or `null` if no row matches.
         */
        export async function get(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Self | null>> {
            args.include ??= GeneratedSource.Default.tree
            return await CloesceOrm.fromEnv(env).get<Self>(Meta, args.query, args.include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate all results.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns All matching hydrated rows.
         */
        export async function list(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Student.Self[]>> {
            args.include ??= GeneratedSource.Default.tree;
            return await CloesceOrm.fromEnv(env).list<Self>(Meta, args.query, args.include);
        }

        /**
         * Hydrate an already-fetched `D1Result` into `Student` rows.
         *
         * @param result - The raw D1 query result to hydrate.
         * @param include - Include tree controlling nested object assembly. Defaults to `Default.tree`.
         * @returns The hydrated rows.
         */
        export function map(result: D1Result, include: IncludeTree<Self> = GeneratedSource.Default.tree): Self[] {
            return CloesceOrm.map<Self>(Meta, result, include);
        }

        /**
         * Hydrate a partial `Student` into a full row, loading the included relations.
         *
         * @param env - Environment with the required binding(s).
         * @param base - Partial row with at least the primary/route key fields populated.
         * @param include - Include tree controlling which relations to load. Defaults to `Default.tree`.
         */
        export async function hydrate(env: { db: CfEnv["db"] }, base: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self>> {
            return await CloesceOrm.fromEnv(env).hydrate<Self>(Meta, base, include);
        }
    }
}
/**
 * Generated surface for the `Dog` model.
 *
 * Implement via {@link impl}. Access data through data sources, not this namespace directly.
 *
 * @example
 * ```ts
 * export const Dog = clo.Dog.impl({
 *   async someApi(self, env, arg) {
 *     const row = await Dog.Default.get(env, arg);
 *     return this.otherApi(self, env);
 *   },
 * });
 * ```
 */
export namespace Dog {
    /** @internal */
    export const Tag = "Dog" as const;
    /** @internal */
    export const Meta = cidl.models.Dog as any;

    /** Hydrated row type. Passed as `self` to instance methods and returned by data-source reads. */
    export interface Self {
        id: number;
        personId: number;
        person: Person.Self;
    }

    /** API methods to implement. Instance methods receive `self`; static methods do not. Each returns an {@link ApiResult}. */
    export interface Api {
        testMethod(
            self: Dog.Self,
        ): ApiResult<void>;
    }
    /** @internal */
    export const _api = undefined as unknown as Api;

    /** Data sources with custom (stubbed) CRUD that you must implement. */
    export interface Sources {
    }

    /**
     * Generated data sources with ready-to-call `get` / `list` / `save` operations.
     *
     * Prefer these over {@link Orm}. Use `Orm` only when you need a custom include tree or query.
     */
    export namespace GeneratedSource {
        /** The `Default` data source. Provides `get`, `list`, and `save` operations. */
        export const Default = {
            /** The include tree this data source hydrates. */
            tree: {"person":{"dogs":{}}},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "Dog"."id" AS "id", "Dog"."personId" AS "personId", "Person_1"."id" AS "person.id", "Dog_2"."id" AS "person.dogs.id", "Dog_2"."personId" AS "person.dogs.personId" FROM "Dog" LEFT JOIN "Person" AS "Person_1" ON "Dog"."personId" = "Person_1"."id" LEFT JOIN "Dog" AS "Dog_2" ON "Person_1"."id" = "Dog_2"."personId"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, id: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Dog"."id" AS "id", "Dog"."personId" AS "personId", "Person_1"."id" AS "person.id", "Dog_2"."id" AS "person.dogs.id", "Dog_2"."personId" AS "person.dogs.personId" FROM "Dog" LEFT JOIN "Person" AS "Person_1" ON "Dog"."personId" = "Person_1"."id" LEFT JOIN "Dog" AS "Dog_2" ON "Person_1"."id" = "Dog_2"."personId" WHERE "Dog"."id" = ?1`).bind(id);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_id: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "Dog"."id" AS "id", "Dog"."personId" AS "personId", "Person_1"."id" AS "person.id", "Dog_2"."id" AS "person.dogs.id", "Dog_2"."personId" AS "person.dogs.personId" FROM "Dog" LEFT JOIN "Person" AS "Person_1" ON "Dog"."personId" = "Person_1"."id" LEFT JOIN "Dog" AS "Dog_2" ON "Person_1"."id" = "Dog_2"."personId" WHERE "Dog"."id" > ?1 ORDER BY "Dog"."id" ASC LIMIT ?2`).bind(lastSeen_id, limit);
            },
            /**
             * Load a single `Dog`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, id: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, id);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `Dog` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_id: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_id, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `Dog` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
    }

    /**
     * Register your implementation of `Dog`.
     *
     * Use `this` to call sibling methods and access generated data sources.
     *
     * @param implObj - Object implementing each {@link Api} method and any custom {@link Sources}.
     * @returns The bound implementation that Cloesce dispatches requests to.
     *
     * @example
     * ```ts
     * export const Dog = clo.Dog.impl({
     *   async myApi(self, env, arg) {
     *     const res = await Dog.Default.get(env, arg);
     *     return res.data!;
     *   },
     * });
     * ```
     */
    export function impl<Impl extends Api & Sources>(implObj: Impl & ThisType<{ tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default } & Impl>): { tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default } & Impl {
        return _impl("Dog", { Orm, Default: GeneratedSource.Default }, implObj) as any;
    }

    /**
     * Lower-level ORM access with explicit {@link IncludeTree}. Returns raw `CloesceResult`.
     *
     * Prefer data sources (e.g. `Dog.Default.save(...)`) unless you need a custom tree or query.
     */
    export namespace Orm {
        /**
         * Insert or update `Dog` and its included relations.
         *
         * @param env - Environment with the required database binding(s).
         * @param newModel - Partial row data to upsert.
         * @param include - Include tree controlling which relations to persist. Defaults to `Default.tree`.
         * @returns The saved row, or `null` if the target vanished.
         */
        export async function save(env: { db: CfEnv["db"] }, newModel: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self | null>> {
            return await CloesceOrm.fromEnv(env).upsert<Self>(Meta, newModel, include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate the first result.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns The hydrated row, or `null` if no row matches.
         */
        export async function get(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Self | null>> {
            args.include ??= GeneratedSource.Default.tree
            return await CloesceOrm.fromEnv(env).get<Self>(Meta, args.query, args.include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate all results.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns All matching hydrated rows.
         */
        export async function list(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Dog.Self[]>> {
            args.include ??= GeneratedSource.Default.tree;
            return await CloesceOrm.fromEnv(env).list<Self>(Meta, args.query, args.include);
        }

        /**
         * Hydrate an already-fetched `D1Result` into `Dog` rows.
         *
         * @param result - The raw D1 query result to hydrate.
         * @param include - Include tree controlling nested object assembly. Defaults to `Default.tree`.
         * @returns The hydrated rows.
         */
        export function map(result: D1Result, include: IncludeTree<Self> = GeneratedSource.Default.tree): Self[] {
            return CloesceOrm.map<Self>(Meta, result, include);
        }

        /**
         * Hydrate a partial `Dog` into a full row, loading the included relations.
         *
         * @param env - Environment with the required binding(s).
         * @param base - Partial row with at least the primary/route key fields populated.
         * @param include - Include tree controlling which relations to load. Defaults to `Default.tree`.
         */
        export async function hydrate(env: { db: CfEnv["db"] }, base: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self>> {
            return await CloesceOrm.fromEnv(env).hydrate<Self>(Meta, base, include);
        }
    }
}
/**
 * Generated surface for the `CourseStudent` model.
 *
 * Implement via {@link impl}. Access data through data sources, not this namespace directly.
 *
 * @example
 * ```ts
 * export const CourseStudent = clo.CourseStudent.impl({
 *   async someApi(self, env, arg) {
 *     const row = await CourseStudent.Default.get(env, arg);
 *     return this.otherApi(self, env);
 *   },
 * });
 * ```
 */
export namespace CourseStudent {
    /** @internal */
    export const Tag = "CourseStudent" as const;
    /** @internal */
    export const Meta = cidl.models.CourseStudent as any;

    /** Hydrated row type. Passed as `self` to instance methods and returned by data-source reads. */
    export interface Self {
        courseId: number;
        studentId: number;
        course: Course.Self;
        student: Student.Self;
    }

    /** API methods to implement. Instance methods receive `self`; static methods do not. Each returns an {@link ApiResult}. */
    export interface Api {
    }
    /** @internal */
    export const _api = undefined as unknown as Api;

    /** Data sources with custom (stubbed) CRUD that you must implement. */
    export interface Sources {
    }

    /**
     * Generated data sources with ready-to-call `get` / `list` / `save` operations.
     *
     * Prefer these over {@link Orm}. Use `Orm` only when you need a custom include tree or query.
     */
    export namespace GeneratedSource {
        /** The `Default` data source. Provides `get`, `list`, and `save` operations. */
        export const Default = {
            /** The include tree this data source hydrates. */
            tree: {"course":{"students":{}},"student":{"courses":{}}},
            /** Raw SQL `SELECT` with joins for this data source's include tree. */
            selectQuery: `SELECT "CourseStudent"."courseId" AS "courseId", "CourseStudent"."studentId" AS "studentId", "Course_1"."id" AS "course.id", "CourseStudent_2"."courseId" AS "course.students.courseId", "CourseStudent_2"."studentId" AS "course.students.studentId", "Student_3"."id" AS "student.id", "CourseStudent_4"."courseId" AS "student.courses.courseId", "CourseStudent_4"."studentId" AS "student.courses.studentId" FROM "CourseStudent" LEFT JOIN "Course" AS "Course_1" ON "CourseStudent"."courseId" = "Course_1"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_2" ON "Course_1"."id" = "CourseStudent_2"."courseId" LEFT JOIN "Student" AS "Student_3" ON "CourseStudent"."studentId" = "Student_3"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_4" ON "Student_3"."id" = "CourseStudent_4"."studentId"`,

            /** Build the bound `D1PreparedStatement` for this data source's `get`. */
            getQuery(env: { db: CfEnv["db"] }, courseId: number, studentId: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "CourseStudent"."courseId" AS "courseId", "CourseStudent"."studentId" AS "studentId", "Course_1"."id" AS "course.id", "CourseStudent_2"."courseId" AS "course.students.courseId", "CourseStudent_2"."studentId" AS "course.students.studentId", "Student_3"."id" AS "student.id", "CourseStudent_4"."courseId" AS "student.courses.courseId", "CourseStudent_4"."studentId" AS "student.courses.studentId" FROM "CourseStudent" LEFT JOIN "Course" AS "Course_1" ON "CourseStudent"."courseId" = "Course_1"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_2" ON "Course_1"."id" = "CourseStudent_2"."courseId" LEFT JOIN "Student" AS "Student_3" ON "CourseStudent"."studentId" = "Student_3"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_4" ON "Student_3"."id" = "CourseStudent_4"."studentId" WHERE ("CourseStudent"."courseId", "CourseStudent"."studentId") = (?1, ?2)`).bind(courseId, studentId);
            },
            /** Build the bound `D1PreparedStatement` for this data source's `list`. */
            listQuery(env: { db: CfEnv["db"] }, lastSeen_courseId: number, lastSeen_studentId: number, limit: number): D1PreparedStatement {
                return env.db.prepare(`SELECT "CourseStudent"."courseId" AS "courseId", "CourseStudent"."studentId" AS "studentId", "Course_1"."id" AS "course.id", "CourseStudent_2"."courseId" AS "course.students.courseId", "CourseStudent_2"."studentId" AS "course.students.studentId", "Student_3"."id" AS "student.id", "CourseStudent_4"."courseId" AS "student.courses.courseId", "CourseStudent_4"."studentId" AS "student.courses.studentId" FROM "CourseStudent" LEFT JOIN "Course" AS "Course_1" ON "CourseStudent"."courseId" = "Course_1"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_2" ON "Course_1"."id" = "CourseStudent_2"."courseId" LEFT JOIN "Student" AS "Student_3" ON "CourseStudent"."studentId" = "Student_3"."id" LEFT JOIN "CourseStudent" AS "CourseStudent_4" ON "Student_3"."id" = "CourseStudent_4"."studentId" WHERE ("CourseStudent"."courseId", "CourseStudent"."studentId") > (?1, ?2) ORDER BY "CourseStudent"."courseId" ASC, "CourseStudent"."studentId" ASC LIMIT ?3`).bind(lastSeen_courseId, lastSeen_studentId, limit);
            },
            /**
             * Load a single `CourseStudent`.
             *
             * @returns An {@link HttpResult} with the row, 404 if not found, or 400 on validation errors.
             */
            async get(env: { db: Env["db"] }, courseId: number, studentId: number): Promise<HttpResult<Self | null>> {
                const stmt = this.getQuery(env, courseId, studentId);
                const res = await CloesceOrm.fromEnv(env).get<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
            /**
             * Load all matching `CourseStudent` rows.
             *
             * @returns An {@link HttpResult} with the rows, or 400 on validation errors.
             */
            async list(env: { db: Env["db"] }, lastSeen_courseId: number, lastSeen_studentId: number, limit: number): Promise<HttpResult<Self[]>> {
                const stmt = this.listQuery(env, lastSeen_courseId, lastSeen_studentId, limit);
                const res = await CloesceOrm.fromEnv(env).list<Self>(Meta, stmt, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                return HttpResult.ok(200, res.value!);
            },
            /**
             * Insert or update a `CourseStudent` and its included relations.
             *
             * @returns An {@link HttpResult} with the saved row, 404 if target vanished, or 400 on validation errors.
             */
            async save(env: { db: Env["db"] }, model: DeepPartial<Self>): Promise<HttpResult<Self | null>> {
                let res = await CloesceOrm.fromEnv(env).upsert<Self>(Meta, model, this.tree);
                if (res.errors.length > 0) {
                    return HttpResult.fail(400, CloesceError.displayErrors(res));
                }
                if (res.value === null) {
                    return HttpResult.fail(404);
                }
                return HttpResult.ok(200, res.value);
            },
        };
    }

    /**
     * Register your implementation of `CourseStudent`.
     *
     * Use `this` to call sibling methods and access generated data sources.
     *
     * @param implObj - Object implementing each {@link Api} method and any custom {@link Sources}.
     * @returns The bound implementation that Cloesce dispatches requests to.
     *
     * @example
     * ```ts
     * export const CourseStudent = clo.CourseStudent.impl({
     *   async myApi(self, env, arg) {
     *     const res = await CourseStudent.Default.get(env, arg);
     *     return res.data!;
     *   },
     * });
     * ```
     */
    export function impl<Impl extends Api & Sources>(implObj: Impl & ThisType<{ tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default } & Impl>): { tag: string; Orm: typeof Orm; Default: typeof GeneratedSource.Default } & Impl {
        return _impl("CourseStudent", { Orm, Default: GeneratedSource.Default }, implObj) as any;
    }

    /**
     * Lower-level ORM access with explicit {@link IncludeTree}. Returns raw `CloesceResult`.
     *
     * Prefer data sources (e.g. `CourseStudent.Default.save(...)`) unless you need a custom tree or query.
     */
    export namespace Orm {
        /**
         * Insert or update `CourseStudent` and its included relations.
         *
         * @param env - Environment with the required database binding(s).
         * @param newModel - Partial row data to upsert.
         * @param include - Include tree controlling which relations to persist. Defaults to `Default.tree`.
         * @returns The saved row, or `null` if the target vanished.
         */
        export async function save(env: { db: CfEnv["db"] }, newModel: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self | null>> {
            return await CloesceOrm.fromEnv(env).upsert<Self>(Meta, newModel, include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate the first result.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns The hydrated row, or `null` if no row matches.
         */
        export async function get(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<Self | null>> {
            args.include ??= GeneratedSource.Default.tree
            return await CloesceOrm.fromEnv(env).get<Self>(Meta, args.query, args.include);
        }

        /**
         * Run a custom `D1PreparedStatement` and hydrate all results.
         *
         * @param env - Environment with the required database binding(s).
         * @param args - Optional `query` (defaults to this model's select) and `include` tree.
         * @returns All matching hydrated rows.
         */
        export async function list(env: { db: CfEnv["db"] }, args: { query?: D1PreparedStatement, include?: IncludeTree<Self> }): Promise<CloesceResult<CourseStudent.Self[]>> {
            args.include ??= GeneratedSource.Default.tree;
            return await CloesceOrm.fromEnv(env).list<Self>(Meta, args.query, args.include);
        }

        /**
         * Hydrate an already-fetched `D1Result` into `CourseStudent` rows.
         *
         * @param result - The raw D1 query result to hydrate.
         * @param include - Include tree controlling nested object assembly. Defaults to `Default.tree`.
         * @returns The hydrated rows.
         */
        export function map(result: D1Result, include: IncludeTree<Self> = GeneratedSource.Default.tree): Self[] {
            return CloesceOrm.map<Self>(Meta, result, include);
        }

        /**
         * Hydrate a partial `CourseStudent` into a full row, loading the included relations.
         *
         * @param env - Environment with the required binding(s).
         * @param base - Partial row with at least the primary/route key fields populated.
         * @param include - Include tree controlling which relations to load. Defaults to `Default.tree`.
         */
        export async function hydrate(env: { db: CfEnv["db"] }, base: DeepPartial<Self>, include: IncludeTree<Self> = GeneratedSource.Default.tree): Promise<CloesceResult<Self>> {
            return await CloesceOrm.fromEnv(env).hydrate<Self>(Meta, base, include);
        }
    }
}

/** @internal */
function _impl(modelName: string, extras: Record<string, any>, implObj: any) {
    const base: any = { ...implObj, ...extras, tag: modelName };
    for (const key of Object.keys(base)) {
        const v = base[key];
        if (typeof v === "function") base[key] = v.bind(base);
    }
    return base;
}

/** @internal */
function _implDs(generated: Record<string, any>, user: Record<string, any>) {
    const merged: any = { ...generated, ...user };
    for (const key of Object.keys(merged)) {
        const v = merged[key];
        if (typeof v === "function") merged[key] = v.bind(merged);
    }
    return merged;
}

import cidl from "./cidl.json" with { type: "json" };

/**
 * Build the Cloesce application. Upgrades {@link CfEnv} and wires up routing.
 *
 * @param env - The raw Cloudflare Workers environment.
 * @returns A {@link CloesceApp} ready to handle requests via `app.run(request)`.
 *
 * @example Custom fetch handler
 * ```ts
 * export default {
 *   async fetch(request: Request, env: CfEnv): Promise<Response> {
 *     const app = cloesce(env);
 *     return app.run(request);
 *   },
 * };
 * ```
 */
export function cloesce(env: CfEnv): CloesceApp {
    // @ts-expect-error
    return new CloesceApp(cidl as any, "http://localhost:5716/api", upgradeEnv(env));
}

// Default entrypoint for a Cloesce app.
// Replace with a custom fetch handler to register API implementations, add middleware, etc.
export default {
    async fetch(request: Request, env: CfEnv): Promise<Response> {
        const app = cloesce(env);
        return await app.run(request);
    }
}