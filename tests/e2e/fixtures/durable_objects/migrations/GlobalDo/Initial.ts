// GENERATED BY CLOESCE. DO NOT MODIFY.
import type { SqlStorage } from "@cloudflare/workers-types";

/** @internal Apply schema changes to the DO's SQLite storage. */
function up(sql: SqlStorage): void {
  sql.exec(`
    --- Cloesce Temporary Table
CREATE TABLE IF NOT EXISTS "$cloesce_tmp" (
  "path" text PRIMARY KEY,
  "primary_key" text NOT NULL
);
  `);
}

/**
 * The `out.Initial` migration for a DO's embedded SQLite database.
 *
 * Pass to `this.cloesce(env, [...])` in your DO constructor. Applied once, in order, before serving requests.
 *
 * @example
 * ```ts
 * import out.InitialMigration from "../../migrations/MyDo/out.Initial_0.js";
 *
 * export class MyDo extends clo.MyDo {
 *   private app: CloesceApp;
 *
 *   constructor(ctx: DurableObjectState, env: clo.CfEnv) {
 *     super(ctx, env);
 *     this.app = this.cloesce(env, [out.InitialMigration]);
 *     this.app.register(MyModel);
 *   }
 * }
 * ```
 */
export default {
  name: "out.Initial",
  timestamp: 0,
  id: "out.Initial_0",
  up,
};