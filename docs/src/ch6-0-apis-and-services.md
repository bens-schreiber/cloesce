# APIs and Services

This chapter covers how to define APIs in Cloesce, typed REST specifications that are generated as backend stubs and frontend client methods, automatically routed by the Cloesce runtime.

Additionally, this chapter talks about Services, which allow developers to write APIs that are not directly tied to a Model.